import sys, os, os.path
from stat import *
os.system('/* unlink - remove a directory entry */')
os.system('/* Should only be used to remove directories by a superuser prepared to let')
os.system('fsck clean up the file system. */')
os.system('/*')
