import sys, os, os.path
from stat import *
os.system('/* Sample builtin to be dynamically loaded with enable -f and create a new')
os.system('builtin. */')
os.system('/* See Makefile for compilation details. */')
#include <config.h>
#if defined (HAVE_UNISTD_H)
#  include <unistd.h>
#endif
#include <stdio.h>
#include "builtins.h"
#include "shell.h"
#include "bashgetopt.h"
os.system('/* A builtin  os.popen('xxx' is normally implemented with an ').read() xxx_builtin' function.
 converting a command that uses the normal Unix argc/argv')
