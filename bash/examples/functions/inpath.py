import sys, os, os.path
from stat import *
def inpath () 
{ 
    os.system('local PROG')
    
    path=$(echo PATH | sed 's/^:/.:/

    
    for x in [path]:
            -x x/sys.argv[1]  && { 
            PROG=x/sys.argv[1]
            
            break
        }
    
    -n "" + PROG + "" 
}
