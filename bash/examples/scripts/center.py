import sys, os, os.path
from stat import *
#
# center - center a group of lines
#
# tabs in the lines might cause this to look a little bit off
#
#
width=${COLUMNS:-80}
if (len(sys.argv) == 0 ):
    os.system('set -- /dev/stdin')
for file in ["" + $ + "@"]:
    while (-r = raw_input()):
        print( "%*s\n" % (( (width+${#REPLY})/2 ), "" + REPLY + "") )
 < $file
exit(0)
